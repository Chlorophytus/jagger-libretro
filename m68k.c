#include "m68k.h"

// TODO: how do we check for the modes and stuff
// in case someone wants to pick this up see Microprocessor Interface
// in Atari Jaguar ver. 8 devmanual...or some place.
// page 8

unsigned int jagger_m68k_read_byte(unsigned int address) {
    // don't compile
}

unsigned int jagger_m68k_read_word(unsigned int address) {
    // don't compile
}

unsigned int jagger_m68k_read_int(unsigned int address) {
    // don't compile
}